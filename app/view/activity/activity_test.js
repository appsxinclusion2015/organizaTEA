'use strict';

describe('myApp.activity module', function() {

  beforeEach(module('myApp.activity'));

  describe('activity controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ctrl = $controller('ActivityController');
      expect(ctrl).toBeDefined();
    }));

  });
});
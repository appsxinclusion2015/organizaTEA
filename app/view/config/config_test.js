'use strict';

describe('myApp.config module', function() {

  beforeEach(module('myApp.config'));

  describe('config controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ctrl = $controller('ConfigController');
      expect(ctrl).toBeDefined();
    }));

  });
});
'use strict';

describe('myApp.routine module', function() {

  beforeEach(module('myApp.routine'));

  describe('routine controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ctrl = $controller('RoutineController');
      expect(ctrl).toBeDefined();
    }));

  });
});